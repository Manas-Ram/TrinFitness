

module.exports = {
    googleClientID: process.env.GOOGLE_CLIENT_ID, //replace with the id provided by your credentials
    googleClientSecret: process.env.GOOGLE_CLIENT_SECRET, //reaplce with the google client secret provided by your credentials
    sessionSecret: process.env.SESSION_SECRET //replace with the secret provided in the README file
};
