<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard</title>
    <link rel="stylesheet" href="teacherdashboard.css">
    <style>
        body {
            display: flex;
            justify-content: space-between;
            padding: 20px;
        }
        .requests {
            width: 48%;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
            margin-right: 2%;
        }
        .request {
            border-bottom: 1px solid #eee;
            padding: 10px;
        }
        .calendar {
            display: flex;
            flex-direction: column;
            width: 48%;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
            padding: 10px;
        }
        .day {
            margin-bottom: 10px;
        }
        .time-slots {
            display: flex;
            flex-direction: column;
        }
        .time-slot {
            padding: 5px;
            border-top: 1px solid #f0f0f0;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <a class="navbar-brand" href="/">
          </a>
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href='/adminstatus'> Admin Status</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id = "calendar"href='/calendar'> Calendar</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" id = "demos"href='/demos'> Demos</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id = "lunchplan"href='/lunchplan'> lunchplan </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id = "machine"href='/machine/bench-press'> Machine </a>
              </li>
    
              <li class="nav-item">
                <a class="nav-link" id = "menu"href='/menu'>Menu</a>
              </li>
    
              <li class="nav-item">
                <a class="nav-link" id = ""href='/teacherdashboard'> Teacher Dashboard</a>
              </li>
          </ul>
        </div>
      </nav>

<div class="requests">
    <h2>Wellness Center Requests</h2>
    <% wellnessRequests.forEach(function(request) { %>
        <div class="request" data-request-id="<%= request.id %>">
            <p><%= request.time %> - <%= request.student %></p>
            <button class="btn accept">Accept</button>
            <button class="btn deny">Deny</button>
        </div>
    <% }); %>
</div>

<div class="calendar">
    <h2>Weekly Calendar</h2>
    <% weeklyCalendar.forEach(function(day) { %>
        <div class="day">
            <h3><%= day.name %></h3>
            <% day.timeSlots.forEach(function(slot) { %>
                <div class="time-slot"><%= slot %></div>
            <% }); %>
        </div>
    <% }); %>
</div>
<script>
    document.querySelectorAll('.btn.accept, .btn.deny').forEach(button => {
        button.addEventListener('click', function() {
            const action = this.classList.contains('accept') ? 'accept' : 'deny';
            const requestId = this.parentElement.getAttribute('data-request-id');
            fetch('/handle-request', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ requestId, action }),
            })
            .then(response => response.json())
            .then(data => {
                if(data.success) {
                    alert('Request ' + action + 'ed');
                }
            })
            .catch(error => console.error('Error:', error));
        });
    });
    </script>
    

</body>
</html>
